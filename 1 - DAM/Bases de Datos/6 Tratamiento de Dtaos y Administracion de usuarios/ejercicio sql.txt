DECLARE
    PRIMERA NUMBER := 5.0; 
    SIGUIENTE NUMBER NOT NULL; 
    FIJA CONSTANT VARCHAR(20); 
    CADENA VARCHAR;
    VALOR1 NUMBER NOT NULL := 4; 
    VALOR2 NUMBER := VALOR1 / 2; 
    VARLOR3 NUMBER := VALOR4 + VALOR2; -- USA UNA VARIABLE LA CUAL SE CREA DESPUES
    VALOR4 NUMBER := DEFAULT 5; -- USA 2 VECES DEFAULT
    CAD VARCHAR(10);
    PROXIMO VALOR1%TYPE; 
    NUMERO NUMBER(4, 2) := 150.3; -- SUPERA EL MAXIMO DE CARACTERES
    CAD VARCHAR(12);
    CAD2 VARCHAR(2) = 'HOLA'; -- SUPERA EL MAXIMO DE CARACTERES
    4NUM NUMBER;
BEGIN;